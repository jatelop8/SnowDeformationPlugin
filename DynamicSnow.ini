[General]
; Max footprints/snow piles kept alive (stamping trail length vs performance).
; Higher = longer trail persists but heavier per-stamp rebuild cost.
; Measured: fp>400 pushes RebuildField from ~5ms to ~10ms (frame hit starts here).
; Default 400 = ~10s trail + capped cost. Range: 100 - 2000.
MaxFootprints=400
